# Technical Architecture

## Current
Static HTML/CSS/JS + Three.js CDN, deployed as a static site on Netlify.

## Target
Component-based frontend, structured content source, reusable 3D scene system, static generation where practical, accessibility-first semantic HTML, and measurable performance/security budgets.

The prototype is intentionally simpler than the target system so that narrative assumptions can be tested before infrastructure grows.
