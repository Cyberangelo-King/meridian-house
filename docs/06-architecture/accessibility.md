# Accessibility

Accessibility is architectural.

Requirements: semantic landmarks, heading hierarchy, keyboard navigation, visible focus, accessible dialogs, sufficient contrast, descriptive controls, touch-friendly targets, reduced-motion support, WebGL-independent content, no colour-only information, sensible mobile reading order.

The immersive layer must never be the only way to understand Meridian.
