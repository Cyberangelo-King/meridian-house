# Performance

## Prototype
Keep HTML small, avoid blocking assets, skip unnecessary 3D for reduced-motion users, use responsive geometry counts, dispose of Three.js resources, optimise imagery, and measure before adding libraries.

## Production budgets
Define budgets for HTML/CSS/JS, image payload, 3D assets, meaningful render, interaction readiness, layout shift, and WebGL frame rate after baseline measurement.
