# Accessibility Testing

Automated checks are a baseline; manual testing is required.

Test keyboard navigation, visible focus, screen-reader landmarks/headings, dialog announcement and dismissal, reduced motion, no hover-only interactions, and meaningful content when WebGL is unavailable.
