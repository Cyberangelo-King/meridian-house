# 02 — Initial Prototype

Built semantic HTML, editorial CSS, scroll-driven methodology, project dossier dialogs, conversation states, a procedural Three.js city, responsive layout, reduced-motion handling, and baseline Netlify headers.

Known limitations: procedural rather than final 3D, CDN dependency, hard-coded content, no conversation backend, incomplete browser/device QA, and production CSP/dependency pinning still pending.
